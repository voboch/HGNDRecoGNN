# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# PyTorch (install the variant matching your CUDA/CPU/MPS target from pytorch.org)
# CPU-only example:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1

# PyTorch Geometric and its dependencies
# See https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
torch_geometric>=2.4

# Data serialisation
pyarrow>=13.0      # required for parquet cache (load_hits)

# Progress bars
tqdm>=4.65

# Jupyter
jupyterlab>=4.0
ipywidgets>=8.0
